# Union of every project's own requirements, for running gateway.py.
# Each sub-directory keeps its own requirements.txt for standalone runs.
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
PyMySQL==1.1.1
# Session JWTs in portal-frontend-code/Backend/main.py (S14 issues one, every other
# endpoint verifies it).
PyJWT==2.10.1
# S3 image upload in portal-frontend-code/Backend/main.py.
boto3==1.43.67
# fastapi's Form()/UploadFile() parser — same endpoint. Imported by fastapi, not by us.
python-multipart==0.0.20
# fastapi.testclient, used by test_gateway.py — deploy.sh runs it as the self-check.
httpx==0.28.1
